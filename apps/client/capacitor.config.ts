import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.supinfo.cloudy',
  appName: 'cloudy',
  webDir: '../../dist/apps/client',
  bundledWebRuntime: false,
};

export default config;
